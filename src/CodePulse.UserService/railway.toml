[build]
builder = "DOCKERFILE"
dockerfilePath = "src/CodePulse.UserService/Dockerfile"

[deploy]
startCommand = "dotnet CodePulse.UserService.dll"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
